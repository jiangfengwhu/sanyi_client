import Geolocation from '@react-native-community/geolocation';

export const getCurrentPosition = Geolocation.getCurrentPosition;

export const watchPosition = Geolocation.watchPosition;

export const clearWatcher = Geolocation.clearWatch;
